import { describe, it } from 'vitest'

// SPEC §12 — implemented alongside lib/queries.ts.
describe('listMyStandups', () => {
  it.todo('returns nextCursor only when more rows exist')
  it.todo('never repeats a row across pages')
})
