import { describe, it } from 'vitest'

// SPEC §12 — implemented alongside lib/digest.ts.
describe('buildDigestHtml', () => {
  it.todo('includes every member')
  it.todo('marks missing members as "No update"')
  it.todo('surfaces blockers in their own section')
})
