import { describe, it } from 'vitest'

// SPEC §12 — implemented alongside lib/date.ts.
describe('todayInTimezone', () => {
  it.todo('returns the correct date across a UTC-day boundary for Asia/Tokyo')
})
