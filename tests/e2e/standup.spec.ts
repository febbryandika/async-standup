import { test } from '@playwright/test'

// SPEC §12 — the full flow needs auth and seeded data (SPEC §13 step 11).
test.skip('register → create team → post standup → it appears in the team feed', () => {})
